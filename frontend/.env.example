# Base URL for the backend API. During local dev with the Vite proxy
# (see vite.config.js) you can leave this unset and requests to /api
# will be forwarded to http://localhost:5000 automatically.
VITE_API_URL=http://localhost:5000/api
